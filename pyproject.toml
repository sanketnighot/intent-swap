[project]
name = "intent-swap-agent"
version = "0.1.0"
description = "IntentSwap execution agent"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "python-dotenv>=1.0.1",
  "web3>=7.0.0",
]

[tool.uv]
package = false
